[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "komply"
version = "0.1.0"
description = "XML policy-based source compliance scanner."
requires-python = ">=3.10"
